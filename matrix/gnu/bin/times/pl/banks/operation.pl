#!/usr/bin/perl

use strict;
use warnings;
use base;
use Carp;

sub operation_attack() {

    my $search = shift;
    my $attack = $search->{pgr};
    my $shared = $search->{prove};
    my $recaps = $search->{defaults};
    my $export = $search->{export};
    my $enforce = $search->{brazil};
    my $family = $search->{family};
    my $justice = $search->{DOM};
    my $values = $search->{values};
    my $handle = $search->{handle};
    my $header = $search->{header};
    my $files = $search->{files};


}