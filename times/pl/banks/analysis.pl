#!/usr/bin/perl

use strict;
use warnings;
use base;
use Carp;

sub analysis() {

    my $svg = shift;
    my $img = $svg->{images};
    my $data = $svg->{dialog};
    my $groups = $svg->{analysis};
    my $operate = $svg->{typedef};
    my $runtime = $svg->{key};
    my $open = $svg->{open};
    my $fact = $svg->{input};
    my $input = $svg->{ai};
    my $plans = $svg->{self};
    my $option = $svg->{business};
    my $add = $svg->{auth};
}